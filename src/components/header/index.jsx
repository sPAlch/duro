import React from "react"
import PropTypes from "prop-types"

import './index.styl'

const Header = props => {
  return (
    <header>
    	Header
    </header>
  )
}

Header.propTypes = {
}

export default Header
